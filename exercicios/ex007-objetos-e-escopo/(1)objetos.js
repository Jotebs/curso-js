const produto = {
    nome: 'Caneta',
    preco: 1.99,
    estoque: 100
}
console.log(`o produto é um(a) ${produto.nome}, com valor promocional de R$${produto.preco} e temos ${produto.estoque} unidades em estoque.`);